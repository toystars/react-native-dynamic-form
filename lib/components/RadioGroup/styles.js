import {
  textPrimary,
  error,
} from '../../../config/colors';

export default {
  label: {
    marginTop: 10,
    fontSize: 14,
    color: textPrimary,
  },
  radioContainer: {},
  otherRow: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  error: {
    fontSize: 12,
    color: error,
  },
};
