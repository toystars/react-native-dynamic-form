import {
  textPrimary,
} from '../../../config/colors';

export default {
  checkboxContainer: {},
  otherRow: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  switchRow: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 5,
  },
  toggleText: {
    fontSize: 16,
    color: textPrimary,
    marginLeft: 20,
    marginRight: 20,
  },
};
