import {
  textPrimary,
  error,
} from '../../../config/colors';

export default {
  label: {
    marginTop: 10,
    fontSize: 14,
    color: textPrimary,
  },
  checkboxContainer: {},
  otherRow: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  switchRow: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 5,
  },
  toggleText: {
    fontSize: 16,
    color: textPrimary,
    marginLeft: 20,
    marginRight: 20,
  },
  error: {
    fontSize: 12,
    color: error,
  },
};
